/** localStorage key for the persisted saved WebSocket connections. */
export const WS_CONNECTIONS_STORAGE_KEY = "web-dev-websocket-connections";

/** localStorage key for the active WebSocket connection id. */
export const ACTIVE_WS_CONNECTION_ID_KEY = "active-websocket-connection-id";

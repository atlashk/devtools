/** localStorage key for the persisted saved gRPC requests. */
export const GRPC_REQUESTS_STORAGE_KEY = "web-dev-grpc-requests";

/** localStorage key for the active gRPC request id. */
export const ACTIVE_GRPC_REQUEST_ID_KEY = "active-grpc-request-id";

/** Tailwind background class for the gRPC tab dot (brand blue). */
export const GRPC_DOT_COLOR = "bg-sky-500";

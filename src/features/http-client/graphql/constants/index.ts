/** localStorage key for the persisted saved GraphQL requests. */
export const GRAPHQL_REQUESTS_STORAGE_KEY = "web-dev-graphql-requests";

/** localStorage key for the active GraphQL request id. */
export const ACTIVE_GRAPHQL_REQUEST_ID_KEY = "active-graphql-request-id";

/** Tailwind background class for the GraphQL tab dot (brand pink). */
export const GRAPHQL_DOT_COLOR = "bg-pink-500";

/** Default query shown in a new GraphQL request. */
export const DEFAULT_GRAPHQL_QUERY = `query {

}`;

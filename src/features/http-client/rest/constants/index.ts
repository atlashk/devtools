/**
 * Local storage key for the persisted saved REST requests.
 */
export const REST_REQUESTS_STORAGE_KEY = "web-dev-rest-requests";

/**
 * Local storage key for the active rest request ID
 */
export const ACTIVE_REST_REQUEST_ID_KEY = "active-rest-request-id";
